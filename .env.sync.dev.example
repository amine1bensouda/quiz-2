# Exemple DEV — clés fixes pour tests locaux rapides (ne pas utiliser en production).
# Copier vers .env (quiz-main) et recopier SYNC_* dans the-school/.env
# OU exécuter : .\scripts\setup-sync-env.ps1 -Apply

FREE_SITE_URL=http://localhost:3002
FREE_SYNC_API_KEY=dev_sync_api_key_local_only_32chars_min
FREE_SYNC_HMAC_SECRET=dev_sync_hmac_secret_local_only_32chars_min

# --- the-school/.env (mêmes secrets) ---
# SYNC_API_KEY=dev_sync_api_key_local_only_32chars_min
# SYNC_HMAC_SECRET=dev_sync_hmac_secret_local_only_32chars_min
